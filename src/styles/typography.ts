import {StyleSheet} from 'react-native';

export const typography = StyleSheet.create({
  bigTitle: {
    fontSize: 20,
  },
  mediumTitle: {
    fontSize: 18,
  },
  normalText: {
    fontSize: 15,
  },
});
