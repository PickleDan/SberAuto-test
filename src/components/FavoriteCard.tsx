import React from 'react';
import {Text, View} from 'react-native';

type FavoriteCardProps = {};

const FavoriteCard = ({}: FavoriteCardProps) => {
  return (
    <View>
      <Text>FavoriteCard</Text>
    </View>
  );
};

export default FavoriteCard;
